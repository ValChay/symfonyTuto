{% extends 'base.html.twig' %}

{% block title %}Gérer les bien !{% endblock %}

{% block body %}
    <div class="jumbotron txt-center">
        <h1> Bienvenue sur la homepage</h1>
        <p>
            loremmflkmdlkfmdlkfmldkmfldkmflkdmùflksmldlgkjflkgjfdlgjfdlmkjglfdkjgldfkjgldfkjgdfkgjdfiutriropiitueriizeporiezpiupezo;</p>
    </div>
    <div class="row">
        <div class="container col-md-4">
            <h1>Gérer les utilisateur</h1>
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Titre</th>
                    <th>Action</th>
                </tr>
                </thead>
                <tbody>
                {% for property in properties %}
                    <tr>
                        <td>{{ property.title }}</td>
                        <td><a href="{{ path('admin.property.edit',{id: property.id}) }}"
                               class="btn btn-secondary">Editer</a>

                            <form method="post" action="{{ path('admin.property.delete', {id: property.id}) }}"
                                  style="display: inline-block" onsubmit="return confirm('Etes vous vraiment sur ?')">
                                <input type="hidden" name="_method" value="DELETE">
                                {#
                                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ property.id) }}">
                                #}
                                <button class="btn btn-danger">Suppression</button>
                            </form>
                        </td>
                    </tr>
                {% endfor %}

                </tbody>
            </table>
            <tfoot>
            <div class="text-center">
                <a href="{{ path('admin.property.new') }}" class="btn btn-primary">Créer un nouvel utilisateur</a>
            </div>
            </tfoot>
        </div>
        <div class="container col-md-4">
            <h1>Gérer les bien</h1>
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Titre</th>
                    <th>Action</th>
                </tr>
                </thead>
                <tbody>
                {% for property in properties %}
                    <tr>
                        <td>{{ property.title }}</td>
                        <td><a href="{{ path('admin.property.edit',{id: property.id}) }}"
                               class="btn btn-secondary">Editer</a>

                            <form method="post" action="{{ path('admin.property.delete', {id: property.id}) }}"
                                  style="display: inline-block" onsubmit="return confirm('Etes vous vraiment sur ?')">
                                <input type="hidden" name="_method" value="DELETE">
                                {#
                                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ property.id) }}">
                                #}
                                <button class="btn btn-danger">Suppression</button>
                            </form>
                        </td>
                    </tr>
                {% endfor %}

                </tbody>
            </table>
            <div class="text-center">
                <a href="{{ path('admin.property.new') }}" class="btn btn-primary">Créer nouveau bien</a>
            </div>
        </div>


    </div>

{% endblock %}
